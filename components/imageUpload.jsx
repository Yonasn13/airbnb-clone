

export default function Components( flats ) {
    return (
        <>
            <label htmlFor={image}>Add picture of your flat:</label>
            <input type="file" name="image" id="image"/>
        </>
    )
}